# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# PyInstaller build output
build/
dist/
release_assets/
*.manifest

# Release packaging script — kept local on purpose, removed from the repo in
# 0df0029. Recover it with: git show 27b52a3:make_release.py > make_release.py
make_release.py

# Runtime / user data — created next to the exe at runtime, never commit
skins.db
*.db
faceit_key.txt
plugin_versions.json
demo_library.json
faceit_avatars.json
demos_cache/
cs2Server/

# Logs & local backups
*.log
*_backup*/
_databackup_*/
_audit_backup_*/
design_backup_*/

# OS / editor cruft
Thumbs.db
.DS_Store
.idea/
.vscode/
